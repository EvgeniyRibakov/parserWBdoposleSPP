# ИНСТРУКЦИЯ: XPATH ПАРСЕР ЦЕН WB (БЫСТРЫЙ)

## 📋 ОПИСАНИЕ

Новый метод парсинга цен с Wildberries через страницы продавца/бренда.

**Преимущества:**
- ⚡ **10-20x быстрее** старого метода
- 📊 **3 типа цен** вместо 1: до СПП, после СПП, с картой
- 🚀 **100 товаров за раз** вместо по одному
- 🎯 **Меньше блокировок** - естественное поведение
- 🔥 **450 артикулов за ~1 минуту** (вместо 10 минут)

---

## 🚀 БЫСТРЫЙ СТАРТ

### Шаг 1: Найти ссылки на страницы продавцов

Для каждого кабинета нужна ссылка на **страницу продавца** (НЕ карточку товара!):

**Способ 1: Через карточку товара**
1. Открой любой свой товар на WB
2. Найди блок "Продавец: Название"
3. Кликни на название продавца
4. Скопируй URL страницы

**Способ 2: Прямая ссылка**
- По ID продавца: `https://www.wildberries.ru/seller/224650`
- По имени бренда: `https://www.wildberries.ru/brands/68941-likato-professional`

### Шаг 2: Настроить парсер

Открой `parsers/Parser_WB_XPATH.py` и укажи ссылки:

```python
SELLER_URLS = [
    "https://www.wildberries.ru/seller/224650",  # Кабинет 1
    "https://www.wildberries.ru/seller/111111",  # Кабинет 2
    # ... добавь все 6 кабинетов
]
```

### Шаг 3: Запустить парсер

```bash
python parsers/Parser_WB_XPATH.py
```

**При первом запуске:**
- Откроется Chrome с твоим профилем (авторизация сохранена)
- Дождись загрузки страниц
- Парсер автоматически соберет все цены

---

## 📊 РЕЗУЛЬТАТЫ

Файл: `data/prices_xpath_results.xlsx`

**Столбцы:**
- `Артикул` - артикул товара
- `Цена ДО СПП` - старая цена (зачеркнутая)
- `Цена ПОСЛЕ СПП` - текущая цена со скидкой
- `Цена С КАРТОЙ` - цена с картой Wildberries
- `URL продавца` - откуда спарсили

---

## ⚙️ НАСТРОЙКИ

### Тестовый режим

```python
TEST_MODE = True  # Только первая страница (для проверки)
```

### Headless режим (фоновый)

```python
HEADLESS_MODE = True  # Браузер не виден (после первой авторизации)
```

### Максимум страниц

```python
MAX_PAGES = 10  # Защита от бесконечного цикла
```

---

## 🔧 КАК ЭТО РАБОТАЕТ

### 1. Lazy Loading (Ленивая загрузка)

WB не загружает сразу все товары:
- Изначально: ~20 товаров
- При скролле: подгружается еще 20-30
- Парсер автоматически скроллит до конца

### 2. XPath селекторы

Парсер ищет карточки товаров через XPath:
```xpath
//article[contains(@class, 'product-card')]  # Карточки
.//del[contains(@class, "price")]           # Старая цена
.//ins[contains(@class, "price")]           # Текущая цена
.//span[contains(@class, "price-card")]     # Цена с картой
```

### 3. Пагинация

Автоматически переходит на следующие страницы (2, 3, 4...) пока не соберет все товары.

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Captcha

**Симптом:** "Почти готово..." на экране

**Решение:** 
- Парсер ждет 30 секунд
- Реши капчу вручную
- Парсер продолжит автоматически

### Не находит карточки товаров

**Причина:** WB изменил структуру HTML

**Решение:**
1. Открой страницу продавца в Chrome
2. Нажми F12 (DevTools)
3. Найди карточку товара в Elements
4. Скопируй класс карточки
5. Обнови селекторы в `parse_products_from_page()`

### Нет цен

**Причина:** Селекторы цен устарели

**Решение:**
1. В DevTools найди элементы с ценами
2. Посмотри их классы
3. Обнови селекторы в коде

---

## 📈 СРАВНЕНИЕ С СТАРЫМ МЕТОДОМ

| Параметр | Старый метод | XPATH метод |
|----------|--------------|-------------|
| Скорость (450 товаров) | ~10 минут | ~1 минута |
| Количество цен | 1 | 3 |
| Запросов к WB | 450 | 5 |
| Риск блокировки | Высокий | Низкий |
| Параллелизм | Сложно | Легко |

---

## 🚀 ПАРАЛЛЕЛЬНЫЙ ПАРСИНГ 6 КАБИНЕТОВ

Чтобы спарсить все 6 кабинетов за 1 минуту:

**Вариант 1: Все URL в одном парсере**
```python
SELLER_URLS = [
    "https://www.wildberries.ru/seller/1111",
    "https://www.wildberries.ru/seller/2222",
    "https://www.wildberries.ru/seller/3333",
    "https://www.wildberries.ru/seller/4444",
    "https://www.wildberries.ru/seller/5555",
    "https://www.wildberries.ru/seller/6666",
]
```
Время: ~6 минут (по 1 минуте на каждый)

**Вариант 2: 6 параллельных окон Chrome**
1. Запусти 6 копий парсера в разных терминалах
2. В каждом укажи свой `SELLER_URL`
3. Все 6 отработают одновременно

Время: ~1 минута! 🔥

---

## 💡 СОВЕТЫ

1. **Первый запуск:** Используй `TEST_MODE = True` для проверки
2. **Авторизация:** Убедись что профиль Chrome авторизован на WB
3. **Адрес доставки:** Установи правильный адрес в профиле (влияет на цены)
4. **Captcha:** При первом запуске может появиться - реши вручную
5. **Обновление:** Если WB изменит HTML, обнови XPath селекторы

---

## 📞 ПОДДЕРЖКА

При проблемах проверь:
1. ✅ Профиль Chrome авторизован на WB
2. ✅ Ссылки на продавцов корректные
3. ✅ Chrome версии 143+ установлен
4. ✅ Все зависимости установлены (`pip install -r requirements.txt`)

---

## 🎯 ROADMAP

Планы на будущее:
- [ ] Поддержка OZON (аналогичный метод)
- [ ] Автоматическое определение новых селекторов
- [ ] API для программного запуска
- [ ] Telegram-бот для мониторинга цен
- [ ] Сравнение с конкурентами

---

**Успешного парсинга! 🚀**




