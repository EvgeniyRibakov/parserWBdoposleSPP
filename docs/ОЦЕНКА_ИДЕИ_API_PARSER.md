# ОЦЕНКА ИДЕИ: Высокоскоростной API-парсер цен для WB и Ozon

## 📊 ОБЩАЯ ОЦЕНКА ИДЕИ

### ✅ Сильные стороны

1. **Правильная архитектурная стратегия**
   - Фокус на API вместо HTML-парсинга — абсолютно верное решение
   - Трехуровневая архитектура (неофициальные API → официальные API → XPath fallback) логична и масштабируема
   - Учет рисков блокировок и приоритет скорости — соответствует бизнес-требованиям

2. **Реалистичные цели**
   - 1600 SKU за 5-10 минут — достижимо при использовании API с батчингом
   - Обновление каждые 1-2 часа — разумная частота для мониторинга цен
   - Точность 1-2% — реалистичное требование

3. **Хорошее понимание текущего состояния**
   - Четко описаны проблемы текущих решений
   - Известны ограничения официальных API
   - Есть понимание, что нужно найти внутренние эндпоинты

### ⚠️ Потенциальные риски и сложности

1. **Нестабильность неофициальных API**
   - Внутренние XHR/GraphQL эндпоинты могут меняться без предупреждения
   - Требуется постоянный мониторинг и адаптация
   - Нужен надежный fallback механизм

2. **Аутентификация и токены**
   - Внутренние API требуют валидные сессии/cookies
   - Токены могут истекать
   - Нужна система обновления сессий

3. **Блокировки и rate limiting**
   - Даже легитимные API-запросы могут быть ограничены
   - Нужна стратегия ротации и батчинга
   - Возможна необходимость прокси-пула

4. **Ozon — более закрытая платформа**
   - Меньше документации по внутренним API
   - Более строгие меры защиты
   - Возможно, потребуется больше времени на reverse engineering

### 🎯 Оценка достижимости целей

| Цель | Оценка | Комментарий |
|------|--------|-------------|
| Скорость: 1600 SKU за 5-10 мин | ✅ **Достижимо** | При батчинге по 50-100 SKU и параллельных запросах |
| Получение всех типов цен | ⚠️ **Частично** | Зависит от найденных эндпоинтов. Цена после СПП — ключевой вызов |
| Устойчивость к блокировкам | ⚠️ **Требует работы** | Нужна продуманная стратегия ротации и fallback |
| Обновление каждые 1-2 часа | ✅ **Достижимо** | При автоматизации и надежной инфраструктуре |

---

## 🏗️ ПЛАН РЕАЛИЗАЦИИ

### ФАЗА 1: Discovery и Reverse Engineering (2-3 недели)

#### Этап 1.1: Анализ текущих решений
- [x] Изучить существующие парсеры (Parser_WB_API_FAST, Parser_WB_XPATH, Parser_WB_HYBRID)
- [x] Понять, какие данные уже получаются через API
- [x] Определить пробелы (цена после СПП для WB, цена с картой для Ozon)

#### Этап 1.2: Discovery внутренних API Wildberries
**ВАЖНО:** Приоритет на ЛК покупателя ВБ (личный кабинет), а не seller-кабинет!

**Инструменты:**
- DevTools Network tab (основной инструмент - не требует установки)
- Browser extensions (Copy as Python Requests, Requestly)
- mitmproxy (опционально, для продвинутого анализа)

**Действия:**
1. Авторизоваться в личном кабинете WB (как покупатель)
2. Открыть карточку товара с СПП
3. Открыть DevTools (F12) → вкладка Network
4. Перехватить все XHR/Fetch/GraphQL запросы при загрузке страницы
5. Найти запросы, которые возвращают цены (особенно после СПП, без карты)
6. Документировать:
   - URL эндпоинта
   - Метод (GET/POST)
   - Headers (особенно Authorization, Cookies)
   - Body/Query параметры
   - Формат ответа (JSON структура)

**Ожидаемый результат:**
- Список эндпоинтов для получения цен
- Понимание структуры запросов
- Примеры успешных запросов

#### Этап 1.3: Discovery внутренних API Ozon
**Аналогично WB, но с фокусом на:**
- Личный кабинет покупателя Ozon (или seller-кабинет, если нужно)
- Карточки товаров с картой лояльности (зеленая цена)
- Поиск эндпоинтов для финальных цен с картой

**Особенности Ozon:**
- Возможно использование GraphQL
- Более сложная система авторизации
- Может потребоваться больше времени
- 3 кабинета с API-ключами и ID

#### Этап 1.4: Документирование найденных API
Создать документ `docs/API_ENDPOINTS.md` с описанием:
- Эндпоинты для WB (цены, СПП, карта)
- Эндпоинты для Ozon (цены, карта)
- Примеры запросов и ответов
- Требования к аутентификации

---

### ФАЗА 2: Прототипирование и тестирование (1-2 недели)

#### Этап 2.1: Создание модуля для работы с WB API
**Структура:**
```
parsers/api/
├── wb/
│   ├── __init__.py
│   ├── auth.py          # Управление сессиями и токенами
│   ├── prices.py        # Получение цен (официальный API)
│   ├── prices_internal.py  # Получение цен через внутренние API
│   └── spp.py           # Получение цен после СПП
```

**Функциональность:**
- Репликация запросов из браузера (headers, cookies, body)
- Управление сессиями (обновление токенов)
- Батчинг SKU (запросы по 50-100 товаров)
- Обработка ошибок: при критичной ошибке API - логи и остановка работы
- Недоступные товары помечать как "Недоступный товар"
- Обязательная обработка всех товаров (нельзя пропускать)

#### Этап 2.2: Создание модуля для работы с Ozon API
**Аналогичная структура:**
```
parsers/api/
├── ozon/
│   ├── __init__.py
│   ├── auth.py
│   ├── prices.py        # Официальный API
│   ├── prices_internal.py  # Внутренние API для финальных цен
│   └── loyalty.py       # Цены с картой лояльности
```

#### Этап 2.3: Тестирование на малой выборке
- Тест на 10-20 SKU для каждого маркетплейса
- Проверка точности данных (сравнение с ручным парсингом)
- Измерение скорости
- Проверка устойчивости к ошибкам

---

### ФАЗА 3: Оптимизация и масштабирование (1-2 недели)

#### Этап 3.1: Батчинг и параллелизация
**Стратегия батчинга:**
- Группировка SKU по 50-100 штук
- Параллельные запросы (asyncio/aiohttp)
- Ограничение concurrent запросов (не более 10-20 одновременно)

**Реализация:**
```python
async def batch_get_prices(sku_list, batch_size=50):
    batches = [sku_list[i:i+batch_size] 
               for i in range(0, len(sku_list), batch_size)]
    
    async with aiohttp.ClientSession() as session:
        tasks = [get_prices_batch(session, batch) 
                 for batch in batches]
        results = await asyncio.gather(*tasks)
    return results
```

#### Этап 3.2: Управление сессиями и токенами
**Требования:**
- Автоматическое обновление токенов при истечении
- Хранение сессий в безопасном месте (env файл или зашифрованный файл)
- Ротация сессий при необходимости

**Реализация:**
```python
class SessionManager:
    def __init__(self):
        self.sessions = {}  # {platform: {cookies, headers, expires_at}}
    
    def get_valid_session(self, platform):
        if self.is_expired(platform):
            self.refresh_session(platform)
        return self.sessions[platform]
    
    def refresh_session(self, platform):
        # Логика обновления сессии
        pass
```

#### Этап 3.3: Fallback механизм
**Трехуровневая стратегия:**
1. Попытка через внутренний API
2. Если не работает → официальный API
3. Если не работает → XPath (только для критичных полей)

**Реализация:**
```python
def get_price_with_fallback(sku, platform):
    try:
        return get_price_internal_api(sku, platform)
    except InternalAPIFailed:
        try:
            return get_price_official_api(sku, platform)
        except OfficialAPIFailed:
            return get_price_xpath(sku, platform)  # Fallback
```

---

### ФАЗА 4: Интеграция и унификация (1 неделя)

#### Этап 4.1: Создание единого интерфейса
**Унифицированный парсер:**
```python
class UnifiedPriceParser:
    def __init__(self):
        self.wb_parser = WBAPIParser()
        self.ozon_parser = OzonAPIParser()
    
    def parse_all(self, wb_skus, ozon_skus):
        wb_results = self.wb_parser.parse_batch(wb_skus)
        ozon_results = self.ozon_parser.parse_batch(ozon_skus)
        return self.merge_results(wb_results, ozon_results)
```

#### Этап 4.2: Форматирование вывода
- Единый формат данных для обоих маркетплейсов
- Сохранение в Excel (как сейчас)
- Возможность экспорта в JSON/CSV

#### Этап 4.3: Логирование и мониторинг
- Детальные логи всех запросов
- Метрики производительности (время на SKU, успешность запросов)
- Алерты при ошибках

---

### ФАЗА 5: Тестирование и доработка (1 неделя)

#### Этап 5.1: Полное тестирование
- Тест на всех 1600 SKU
- Проверка точности данных
- Проверка скорости (цель: 5-10 минут)
- Стресс-тестирование (множественные запуски)

#### Этап 5.2: Обработка edge cases
- Товары без СПП
- Товары без карты лояльности
- Недоступные товары
- Ошибки API

#### Этап 5.3: Документация
- Обновление README
- Документация API эндпоинтов
- Инструкция по настройке и запуску

---

## 🔧 ТЕХНИЧЕСКИЙ СТЕК

### Рекомендуемые библиотеки

**HTTP клиенты:**
- `aiohttp` — для асинхронных запросов (основной)
- `requests` — для синхронных запросов (fallback)

**Управление сессиями:**
- `requests-cache` — кеширование запросов (опционально)
- `browser-cookie3` — извлечение cookies из браузера (для инициализации)

**Парсинг и обработка:**
- `openpyxl` — работа с Excel (уже используется)
- `pydantic` — валидация данных (рекомендуется)

**Асинхронность:**
- `asyncio` — встроенная библиотека Python
- `aiofiles` — асинхронная работа с файлами

**Мониторинг и логирование:**
- `loguru` — улучшенное логирование
- `tenacity` — retry логика с экспоненциальной задержкой

### Структура проекта (предлагаемая)

```
parsers/
├── api/
│   ├── __init__.py
│   ├── base.py              # Базовый класс для парсеров
│   ├── wb/
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   ├── prices.py
│   │   ├── prices_internal.py
│   │   └── spp.py
│   └── ozon/
│       ├── __init__.py
│       ├── auth.py
│       ├── prices.py
│       └── prices_internal.py
├── unified/
│   ├── __init__.py
│   └── parser.py            # Унифицированный парсер
└── utils/
    ├── __init__.py
    ├── session_manager.py
    ├── batch_processor.py
    └── fallback.py
```

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ

| Фаза | Время | Зависимости |
|------|-------|-------------|
| Фаза 1: Discovery | 2-3 недели | Доступ к seller-кабинетам, инструменты перехвата |
| Фаза 2: Прототипирование | 1-2 недели | Результаты Фазы 1 |
| Фаза 3: Оптимизация | 1-2 недели | Рабочие прототипы |
| Фаза 4: Интеграция | 1 неделя | Оптимизированные модули |
| Фаза 5: Тестирование | 1 неделя | Полная интеграция |
| **ИТОГО** | **6-9 недель** | |

**Примечание:** Время может варьироваться в зависимости от сложности reverse engineering внутренних API.

---

## 🎯 КРИТЕРИИ УСПЕХА

### Минимальные требования (MVP)
- ✅ Получение всех критичных цен (со скидкой, после СПП, с картой) для WB
- ✅ Получение всех критичных цен для Ozon
- ✅ Скорость: 1600 SKU за 15 минут (первая версия)
- ✅ Точность: отклонение не более 2%

### Целевые показатели
- ✅ Скорость: 1600 SKU за 5-10 минут
- ✅ Точность: отклонение не более 1%
- ✅ Устойчивость: успешность запросов > 95%
- ✅ Автоматизация: обновление каждые 1-2 часа без вмешательства

---

## 🚨 РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Внутренние API изменились | Высокая | Критично | Fallback на официальный API + XPath |
| Блокировка запросов | Средняя | Высокое | Ротация сессий, прокси, jitter задержек |
| Токены истекают | Средняя | Среднее | Автоматическое обновление сессий |
| Ozon API сложнее найти | Средняя | Среднее | Больше времени на discovery, возможно XPath fallback |
| Недостаточная скорость | Низкая | Среднее | Оптимизация батчинга и параллелизма |

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. **Немедленно:**
   - Начать Фазу 1 (Discovery) — перехват трафика WB seller-кабинета
   - Настроить mitmproxy или использовать DevTools
   - Документировать найденные эндпоинты

2. **В течение недели:**
   - Завершить discovery для WB
   - Начать discovery для Ozon
   - Создать первый прототип для WB

3. **В течение месяца:**
   - Рабочий прототип для обоих маркетплейсов
   - Тестирование на реальных данных
   - Оптимизация скорости

---

## 💡 РЕКОМЕНДАЦИИ

1. **Начать с WB** — больше информации, проще найти эндпоинты
2. **Использовать существующие решения** — не изобретать велосипед, улучшать текущее
3. **Инкрементальный подход** — сначала получить данные, потом оптимизировать скорость
4. **Документировать все** — найденные эндпоинты, структуры запросов, решения
5. **Тестировать часто** — на каждом этапе проверять работоспособность

---

**Дата создания:** 2025-01-27  
**Ветка:** `apisparse`  
**Статус:** План готов к реализации

