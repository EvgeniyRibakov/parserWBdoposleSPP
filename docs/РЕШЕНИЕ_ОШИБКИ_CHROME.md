# РЕШЕНИЕ ОШИБКИ: "cannot connect to chrome"

## 🔴 **ПРОБЛЕМА**

```
Message: session not created: cannot connect to chrome at 127.0.0.1:60997
from chrome not reachable
```

**Причина:** Chrome запускается, но Selenium не может к нему подключиться.

---

## ✅ **РЕШЕНИЯ (по порядку)**

### **Решение 1: Закрыть все окна Chrome** ⭐ (САМОЕ ПРОСТОЕ)

```powershell
# В PowerShell выполни:
taskkill /F /IM chrome.exe
```

**Затем:**
- Подожди 5 секунд
- Запусти парсер снова

---

### **Решение 2: Перезапуск с очисткой профиля**

Парсер теперь **автоматически**:
- ✅ Проверяет запущенный Chrome
- ✅ Очищает lock-файлы
- ✅ Пробует 3 разные конфигурации запуска

**Просто запусти снова:**
```bash
python parsers/Parser_WB_XPATH.py
```

---

### **Решение 3: Ручная очистка профиля**

Если автоматическая очистка не помогла:

1. Закрой Chrome полностью
2. Удали lock-файлы вручную:
   ```powershell
   cd "D:\Downloads\wb beforeafter spp prices\chrome_parser_profile"
   del SingletonLock
   del lockfile
   del SingletonSocket
   del SingletonCookie
   del DevToolsActivePort
   ```
3. Запусти парсер снова

---

### **Решение 4: Использовать другой профиль**

В `Parser_WB_XPATH.py` измени:
```python
SOURCE_PROFILE_FOR_COPY = "Default"  # Вместо "Profile 4"
```

Или отключи копирование профиля:
```python
COPY_PROFILE_DATA = False
```

---

### **Решение 5: Перезагрузка компьютера**

Если ничего не помогает:
1. Перезагрузи компьютер
2. Запусти парсер первым делом (до открытия Chrome вручную)

---

## 🔧 **ЧТО УЛУЧШЕНО В ПАРСЕРЕ**

### **Автоматические проверки:**
- ✅ Проверка запущенного Chrome
- ✅ Очистка lock-файлов перед запуском
- ✅ 3 попытки запуска с разными настройками
- ✅ Детальные логи ошибок

### **Новые логи:**
```
[ЛОГ] ⚠ Chrome уже запущен (процессов: 5)
[ЛОГ] Очистка lock-файлов профиля...
[ЛОГ] Попытка 1/3 запуска Chrome...
[ЛОГ] Параметры: use_subprocess=True, version_main=None
```

---

## 📋 **ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ**

- [ ] Chrome закрыт полностью
- [ ] Прошло 5+ секунд после закрытия Chrome
- [ ] Антивирус не блокирует (проверь логи)
- [ ] Профиль не поврежден (попробуй другой профиль)

---

## 💡 **СОВЕТЫ**

1. **Всегда закрывай Chrome перед запуском парсера**
2. **Если Chrome завис** - перезагрузи компьютер
3. **Используй видимый режим** (`HEADLESS_MODE = False`) для первого запуска
4. **Проверь антивирус** - может блокировать запуск Chrome через Selenium

---

## 🚀 **БЫСТРОЕ РЕШЕНИЕ**

```powershell
# 1. Закрой Chrome
taskkill /F /IM chrome.exe

# 2. Подожди 5 секунд
timeout /t 5

# 3. Запусти парсер
python parsers/Parser_WB_XPATH.py
```

**В 90% случаев это решает проблему!** ✅

---

## 📞 **ЕСЛИ НЕ ПОМОГЛО**

Пришли:
1. Полный лог ошибки
2. Версию Chrome (`chrome://version/`)
3. Версию Python (`python --version`)
4. Что пробовал из решений выше

