# 📁 Структура проекта

## Обзор

Проект организован по функциональному принципу: код, документация и данные разделены по папкам.

## Детальная структура

```
wb-beforeafter-spp-prices/
│
├── 📂 parsers/                    # Исходный код парсеров
│   ├── Parser_WB_Search.py       # ⭐ Основной парсер (Selenium)
│   ├── Parser_WB_API_FAST.py     # Быстрый парсер через API
│   ├── Parser_WB_Card_API.py     # Парсер через Card API
│   ├── Parser_WB_ALL_PRODUCTS.py # Парсер всех товаров
│   ├── Parser_UNIFIED.py         # Унифицированный парсер
│   ├── Create_Links_Excel.py     # Генератор ссылок
│   └── Step1_Load_All_IDs.py     # Загрузка артикулов
│
├── 📂 docs/                       # Документация проекта
│   ├── ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md  # Инструкция по использованию
│   ├── ИНСТРУКЦИЯ_БЫСТРЫЙ_ПАРСЕР.md
│   ├── ПРАВИЛА_ПРОЕКТА.md        # ⭐ Главные правила
│   ├── ПРАВИЛА_ПАРСИНГА_WB.md    # Правила парсинга цен
│   ├── ПРОБЛЕМА_GIT_БОЛЬШИЕ_ФАЙЛЫ.md
│   └── СТРУКТУРА_ПРОЕКТА.md      # Этот файл
│
├── 📂 data/                       # Данные (Excel файлы)
│   ├── Парсер цен.xlsx            # Входной файл (артикулы)
│   ├── links_to_products.xlsx    # Ссылки (генерируется)
│   └── prices_results.xlsx       # Результаты парсинга
│
├── 📂 code_pages/                 # Примеры HTML для разработки
│   ├── elements/                   # Отдельные элементы
│   │   ├── tap_find_black_price.html
│   │   ├── copy_black_price.html
│   │   └── sold_out_product.html
│   └── pages/                      # Полные страницы
│       └── product_page_example.html
│
├── 📂 chrome_parser_profile/       # Рабочий профиль Chrome
│   ├── Network/Cookies             # Авторизация WB
│   ├── Login Data                 # Сохраненные логины
│   ├── Preferences                # Настройки (адрес доставки)
│   └── ...                        # Кеши, история и т.д.
│
├── 📄 requirements.txt            # Зависимости Python
├── 📄 .gitignore                  # Игнорируемые файлы
└── 📄 README.md                   # Главный README
```

## Описание папок

### `parsers/` - Исходный код
**Назначение**: Все Python скрипты парсеров и вспомогательные утилиты

**Файлы**:
- `Parser_WB_Search.py` - Основной парсер через Selenium (рекомендуется)
- `Parser_WB_API_FAST.py` - Быстрый парсер через API
- `Create_Links_Excel.py` - Создает файл со ссылками из артикулов
- Остальные - альтернативные реализации

**Особенности**:
- Все парсеры используют относительные пути к `data/`
- Автоматически определяют корень проекта через `os.path.dirname(__file__)`

### `docs/` - Документация
**Назначение**: Вся документация проекта

**Содержимое**:
- Инструкции по использованию
- Правила и стандарты проекта
- Решения проблем
- Описание структуры

**Важно**: Вся документация на русском языке

### `data/` - Данные
**Назначение**: Excel файлы с входными данными и результатами

**Файлы**:
- `Парсер цен.xlsx` - Входной файл с артикулами товаров
- `links_to_products.xlsx` - Генерируется автоматически
- `prices_results.xlsx` - Результаты парсинга

**Особенности**:
- Входные файлы в `.gitignore` (личные данные клиентов)
- Результаты тоже в `.gitignore`
- Только шаблоны можно коммитить

### `code_pages/` - Примеры HTML
**Назначение**: Примеры HTML элементов для разработки парсеров

**Структура**:
- `elements/` - Отдельные элементы страниц (кнопки, цены)
- `pages/` - Полные примеры страниц товаров

**Использование**: Для разработки и тестирования селекторов

### `chrome_parser_profile/` - Профиль Chrome
**Назначение**: Рабочий профиль браузера для парсинга

**Содержимое**:
- Cookies для авторизации на WB
- Настройки браузера
- Кеши (НЕ коммитятся в git!)

**Особенности**:
- Создается автоматически при первом запуске
- Копируются данные из Profile 4 пользователя
- Требует ручной авторизации при первом запуске
- В `.gitignore` (огромные файлы, личные данные)

## Пути в коде

### Как парсеры находят файлы:

```python
# Определение корня проекта
PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_DIR = os.path.join(PROJECT_ROOT, "data")

# Пути к Excel файлам
EXCEL_FILE = os.path.join(DATA_DIR, "Парсер цен.xlsx")
OUTPUT_FILE = os.path.join(DATA_DIR, "prices_results.xlsx")

# Путь к Chrome профилю
TEMP_PROFILE_DIR = os.path.join(PROJECT_ROOT, "chrome_parser_profile")
```

**Преимущества**:
- ✅ Работает из любой папки
- ✅ Не зависит от текущей директории
- ✅ Легко переносится между компьютерами

## Запуск парсеров

### Из корня проекта:
```bash
python parsers/Parser_WB_Search.py
```

### Из папки parsers:
```bash
cd parsers
python Parser_WB_Search.py
```

**Оба способа работают!** Парсеры автоматически находят нужные файлы.

## Git и структура

### Что коммитится:
- ✅ Код в `parsers/`
- ✅ Документация в `docs/`
- ✅ Примеры в `code_pages/`
- ✅ `requirements.txt`
- ✅ `.gitignore`, `README.md`

### Что НЕ коммитится:
- ❌ `chrome_parser_profile/` (огромные файлы, личные данные)
- ❌ `data/*.xlsx` (личные данные клиентов)
- ❌ Кеши и временные файлы

## Изменение структуры

Если нужно изменить структуру:

1. **Обновить пути** в парсерах (см. раздел "Пути в коде")
2. **Обновить `.gitignore`** если нужно
3. **Обновить документацию** (этот файл и README.md)
4. **Протестировать** запуск парсеров

## Миграция со старой структуры

Если у тебя старая версия проекта (все файлы в корне):

1. Создать папки: `parsers/`, `docs/`, `data/`
2. Переместить файлы по папкам
3. Обновить пути в парсерах (уже сделано)
4. Запустить парсер - должно работать!

---

**Последнее обновление**: 11.12.2025



