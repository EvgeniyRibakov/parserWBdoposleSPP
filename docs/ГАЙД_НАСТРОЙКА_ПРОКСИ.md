# ГАЙД: Настройка браузера на прокси 8080

## 🎯 Цель

Настроить браузер для использования прокси `localhost:8080` для перехвата трафика через mitmproxy.

---

## 🌐 Вариант 1: Через командную строку (САМЫЙ ПРОСТОЙ)

### Chrome

**Шаг 1:** Закройте все окна Chrome

**Шаг 2:** Откройте PowerShell или CMD и выполните:

```powershell
# Путь к Chrome (обычно один из этих)
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --proxy-server="http://localhost:8080" --user-data-dir="C:\temp\chrome_proxy"
```

Или если Chrome в другой папке:

```powershell
& "C:\Program Files (x86)\Google\Chrome\Application\chrome.exe" --proxy-server="http://localhost:8080" --user-data-dir="C:\temp\chrome_proxy"
```

**Что происходит:**
- Chrome запустится с прокси `localhost:8080`
- Создастся временный профиль в `C:\temp\chrome_proxy`
- Все запросы будут идти через прокси

**После работы:**
- Закройте Chrome
- Удалите папку `C:\temp\chrome_proxy` (опционально)

---

## 🔧 Вариант 2: Через настройки Windows (для всех браузеров)

### Шаг 1: Откройте настройки прокси Windows

**Windows 10/11:**
1. Нажмите `Win + I` (открыть Настройки)
2. Перейдите: **Сеть и Интернет** → **Прокси**
3. Или найдите "Прокси" в поиске Windows

**Через старый интерфейс:**
1. Нажмите `Win + R`
2. Введите: `inetcpl.cpl` и нажмите Enter
3. Вкладка **Подключения** → кнопка **Настройка сети**

### Шаг 2: Настройте прокси

**В новом интерфейсе (Windows 10/11):**
1. В разделе **Ручная настройка прокси-сервера**
2. Включите переключатель **Использовать прокси-сервер**
3. Заполните:
   - **Адрес:** `localhost` или `127.0.0.1`
   - **Порт:** `8080`
4. **НЕ** ставьте галочку "Не использовать прокси-сервер для локальных адресов" (оставьте пустым)
5. Нажмите **Сохранить**

**В старом интерфейсе:**
1. Выберите **Использовать прокси-сервер для локальных подключений**
2. **Адрес:** `localhost`
3. **Порт:** `8080`
4. Нажмите **OK**

### Шаг 3: Проверка

1. Откройте любой браузер
2. Перейдите на любой сайт
3. Если сайт загружается - прокси работает!

### Шаг 4: Отключение прокси (после работы)

Вернитесь в настройки и **отключите** прокси-сервер.

---

## 🧩 Вариант 3: Расширение браузера (рекомендуется для постоянного использования)

### Chrome: Proxy SwitchyOmega

**Шаг 1:** Установите расширение
1. Откройте Chrome Web Store
2. Найдите "Proxy SwitchyOmega"
3. Установите расширение

**Шаг 2:** Настройте профиль прокси
1. Кликните на иконку Proxy SwitchyOmega в панели расширений
2. Выберите **Настройки** (Options)
3. Нажмите **Новый профиль**
4. Название: `mitmproxy`
5. Тип: **HTTP**
6. Сервер прокси: `127.0.0.1`
7. Порт: `8080`
8. Нажмите **Применить**

**Шаг 3:** Использование
1. Кликните на иконку Proxy SwitchyOmega
2. Выберите профиль `mitmproxy`
3. Все запросы пойдут через прокси
4. После работы выберите **Прямое подключение**

### Firefox: FoxyProxy

**Шаг 1:** Установите расширение
1. Откройте Firefox Add-ons
2. Найдите "FoxyProxy"
3. Установите

**Шаг 2:** Настройте прокси
1. Кликните на иконку FoxyProxy
2. **Настройки** → **Добавить**
3. Название: `mitmproxy`
4. Тип прокси: **HTTP**
5. IP адрес: `127.0.0.1`
6. Порт: `8080`
7. Сохраните

**Шаг 3:** Использование
1. Кликните на иконку FoxyProxy
2. Выберите `mitmproxy`
3. После работы выберите **Отключить прокси**

---

## ✅ Проверка работы прокси

### Способ 1: Через mitmproxy

1. Запустите `python parsers/discovery/start_discovery.py`
2. Откройте браузер с настроенным прокси
3. Перейдите на любой сайт
4. В терминале должны появиться перехваченные запросы

### Способ 2: Через сайт проверки IP

1. Откройте браузер с прокси
2. Перейдите на `https://whatismyipaddress.com/`
3. Если IP изменился - прокси работает

### Способ 3: Через ошибку сертификата

При использовании mitmproxy браузер может показать ошибку сертификата:
- Это **нормально** - mitmproxy использует свой сертификат
- Нужно будет принять сертификат (см. ниже)

---

## 🔒 Решение проблемы с сертификатом (HTTPS)

### Проблема

При использовании mitmproxy браузер может показать ошибку:
```
Ваше подключение не защищено
NET::ERR_CERT_AUTHORITY_INVALID
```

### Решение

**Шаг 1:** Получите сертификат mitmproxy
1. Запустите mitmproxy
2. Откройте браузер и перейдите на `http://mitm.it`
3. Выберите вашу ОС (Windows)
4. Скачайте сертификат `mitmproxy-ca-cert.pem`

**Шаг 2:** Установите сертификат в Windows
1. Дважды кликните на скачанный файл
2. Нажмите **Установить сертификат**
3. Выберите **Текущий пользователь**
4. Нажмите **Далее**
5. Выберите **Поместить все сертификаты в следующее хранилище**
6. Нажмите **Обзор** → выберите **Доверенные корневые центры сертификации**
7. Нажмите **OK** → **Далее** → **Готово**

**Шаг 3:** Перезапустите браузер

---

## 🚀 Быстрый старт (рекомендуемый способ)

### Для одноразового использования:

```powershell
# 1. Запустите discovery
python parsers/discovery/start_discovery.py

# 2. В другом терминале запустите Chrome с прокси
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --proxy-server="http://localhost:8080" --user-data-dir="C:\temp\chrome_proxy"
```

### Для постоянного использования:

1. Установите расширение Proxy SwitchyOmega
2. Настройте профиль `mitmproxy` (127.0.0.1:8080)
3. Включайте/выключайте через иконку расширения

---

## ⚠️ Важные замечания

1. **Прокси должен быть запущен** - если mitmproxy не запущен, браузер не сможет подключиться
2. **Порт 8080 должен быть свободен** - если порт занят, измените порт в скрипте
3. **После работы отключите прокси** - иначе браузер не будет работать без mitmproxy
4. **Используйте отдельный профиль Chrome** - чтобы не мешать основному браузеру

---

## 🆘 Решение проблем

### Проблема: Браузер не подключается

**Решение:**
- Проверьте, что mitmproxy запущен
- Проверьте порт: `netstat -an | findstr 8080`
- Попробуйте другой порт: измените `port=8080` на `port=8888` в скрипте

### Проблема: Все сайты не работают

**Решение:**
- Убедитесь, что mitmproxy запущен
- Проверьте настройки прокси в Windows
- Отключите прокси и попробуйте снова

### Проблема: Ошибка сертификата

**Решение:**
- Установите сертификат mitmproxy (см. раздел выше)
- Или используйте HTTP сайты для теста

---

## 📝 Чек-лист

- [ ] Запущен mitmproxy (`python parsers/discovery/start_discovery.py`)
- [ ] Настроен браузер на прокси `localhost:8080`
- [ ] Установлен сертификат mitmproxy (для HTTPS)
- [ ] Браузер открывается и загружает сайты
- [ ] В терминале mitmproxy видны перехваченные запросы

---

**Готово!** Теперь можно начинать discovery внутренних API.

