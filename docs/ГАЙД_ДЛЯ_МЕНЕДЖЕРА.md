# 📘 Гайд по установке парсера цен Wildberries для менеджера

> **Этот гайд написан специально для людей без опыта программирования.**  
> Все шаги описаны максимально просто, с примерами и решениями проблем.

---

## 🎯 Что делает этот парсер?

Парсер автоматически собирает цены товаров с сайта Wildberries и сохраняет их в Excel файл.  
**Вам не нужно вручную открывать каждый товар и копировать цену** - всё делает программа!

---

## ✅ Что нужно для работы?

### На компьютере должно быть установлено:

1. **Google Chrome** (браузер) - скачать можно [здесь](https://www.google.com/chrome/)
2. **Python** (язык программирования) - скачать можно [здесь](https://www.python.org/downloads/)
3. **Git** (для скачивания проекта) - скачать можно [здесь](https://git-scm.com/download/win)

> ⚠️ **Важно:** При установке Python обязательно отметьте галочку **"Add Python to PATH"**!

---

## 📋 Пошаговая инструкция установки

### ШАГ 1: Скачать проект с GitHub

1. Откройте **PowerShell** или **Командную строку** (CMD)
   - Нажмите `Win + R`, введите `powershell` и нажмите Enter
   - Или найдите "PowerShell" в меню Пуск

2. Перейдите в папку, где хотите разместить проект (например, `D:\Projects\`)
   ```powershell
   cd D:\Projects
   ```

3. Скачайте проект командой:
   ```powershell
   git clone https://github.com/EvgeniyRibakov/parserWBdoposleSPP.git
   ```

4. Перейдите в папку проекта:
   ```powershell
   cd parserWBdoposleSPP
   ```

**Что должно произойти:**
- ✅ Создастся папка `parserWBdoposleSPP`
- ✅ В ней появятся файлы проекта

**Если возникла ошибка:**
- `git: command not found` → Установите Git с [git-scm.com](https://git-scm.com/download/win) и перезапустите PowerShell

---

### ШАГ 2: Скопировать файл с данными

На рабочем компьютере (откуда вы копируете):

1. Найдите файл `Парсер цен.xlsx` в папке `data/`
2. Скопируйте этот файл на флешку или в облако (Google Drive, Яндекс.Диск)

На новом компьютере (куда устанавливаете):

1. Создайте папку `data` в папке проекта (если её нет):
   ```powershell
   mkdir data
   ```

2. Скопируйте файл `Парсер цен.xlsx` в папку `data/`

**Путь должен быть таким:**
```
D:\Projects\parserWBdoposleSPP\data\Парсер цен.xlsx
```

**Проверка:**
- Откройте файл в Excel
- Убедитесь, что есть лист с названием **"Данные для парсера ВБ"**
- В этом листе должна быть колонка с артикулами товаров

---

### ШАГ 3: Установить зависимости Python

В PowerShell выполните команду:

```powershell
pip install -r requirements.txt
```

**Что должно произойти:**
- ✅ Начнётся установка библиотек
- ✅ В конце появится сообщение "Successfully installed ..."
- ✅ Не должно быть ошибок красным цветом

**Если возникла ошибка:**

**Ошибка 1:** `pip: command not found`
```powershell
# Решение: используйте команду с python
python -m pip install -r requirements.txt
```

**Ошибка 2:** `Permission denied`
```powershell
# Решение: запустите PowerShell от имени администратора
# Правый клик на PowerShell → "Запуск от имени администратора"
```

**Ошибка 3:** Долгая установка или зависание
- Это нормально, установка может занять 2-5 минут
- Подождите, не закрывайте окно

**Проверка установки:**
```powershell
python -c "import selenium; import undetected_chromedriver; import openpyxl; print('Все библиотеки установлены!')"
```

Должно вывести: `Все библиотеки установлены!`

---

### ШАГ 4: Создать файл со ссылками на товары

Выполните команду:

```powershell
python parsers/Create_Links_Excel.py
```

**Что должно произойти:**
- ✅ Прочитает артикулы из файла `data/Парсер цен.xlsx`
- ✅ Создаст файл `data/links_to_products.xlsx`
- ✅ Выведет количество созданных ссылок

**Пример вывода:**
```
Найдено артикулов: 925
Создано ссылок: 925
✓ Файл сохранен: data/links_to_products.xlsx
```

**Если возникла ошибка:**

**Ошибка 1:** `FileNotFoundError: data/Парсер цен.xlsx`
- Проверьте, что файл скопирован в папку `data/`
- Проверьте название файла (должно быть точно `Парсер цен.xlsx`)

**Ошибка 2:** `KeyError: 'Данные для парсера ВБ'`
- Откройте файл `Парсер цен.xlsx` в Excel
- Переименуйте лист в **"Данные для парсера ВБ"** (точно так, как написано)

**Ошибка 3:** Файл открыт в Excel
- Закройте файл `Парсер цен.xlsx` в Excel
- Попробуйте снова

---

### ШАГ 5: Закрыть Chrome перед запуском

⚠️ **ВАЖНО:** Перед запуском парсера нужно закрыть все окна Chrome!

Выполните команду:

```powershell
taskkill /F /IM chrome.exe
```

**Что должно произойти:**
- ✅ Все процессы Chrome закроются
- ✅ Может появиться сообщение "SUCCESS" или "Процесс не найден" (это нормально)

**Если Chrome не закрывается:**
- Закройте все вкладки Chrome вручную
- Затем выполните команду снова

---

### ШАГ 6: Запустить парсер

Выполните команду:

```powershell
python parsers/Parser_WB_Search.py
```

**Что должно произойти:**

1. **Инициализация:**
   ```
   ================================================================================
   ПАРСЕР ЦЕН WB - ПРОСТОЙ ПАРСЕР
   ================================================================================
   ✓ Конфигурация проверена
   [1/3] Найдено товаров: 925
   ```

2. **Запуск Chrome:**
   - Откроется окно Chrome (это нормально!)
   - Парсер создаст свой профиль автоматически

3. **Пауза для авторизации** (при первом запуске):
   ```
   ⏸  ПАУЗА ДЛЯ АВТОРИЗАЦИИ
   
   📋 ИНСТРУКЦИЯ:
      1. В открывшемся Chrome зайдите на сайт WB
      2. Авторизуйтесь в своем аккаунте
      3. Установите правильный адрес доставки
      4. Нажмите ENTER когда будете готовы
   ```
   
   **Что делать:**
   - Откройте сайт [wildberries.ru](https://www.wildberries.ru) в открывшемся Chrome
   - Войдите в свой аккаунт (если не авторизованы)
   - Установите адрес доставки (важно для правильных цен!)
   - Вернитесь в PowerShell и нажмите **Enter**

4. **Парсинг товаров:**
   ```
   [3/3] Парсинг цен...
   [1/925] Артикул: 102136669
     ✓ УСПЕХ: 1599 ₽
   [2/925] Артикул: 102141007
     ✓ УСПЕХ: 1299 ₽
   ...
   ```
   
   - Парсер будет открывать товары по очереди
   - Извлекать цены
   - Показывать прогресс

5. **Сохранение результатов:**
   ```
   ФИНАЛЬНОЕ СОХРАНЕНИЕ РЕЗУЛЬТАТОВ
   ✓ Сохранено: 925 товаров
   ✓ Файл: data/prices_results.xlsx
   ```

**Если возникла ошибка:**

**Ошибка 1:** `SessionNotCreatedException: cannot connect to chrome`
```powershell
# Решение:
# 1. Закройте Chrome полностью
taskkill /F /IM chrome.exe
timeout /t 3

# 2. Удалите папку chrome_parser_profile (если есть)
rmdir /s /q chrome_parser_profile

# 3. Запустите парсер снова
python parsers/Parser_WB_Search.py
```

**Ошибка 2:** `FileNotFoundError: data/links_to_products.xlsx`
```powershell
# Решение: сначала создайте файл со ссылками
python parsers/Create_Links_Excel.py
python parsers/Parser_WB_Search.py
```

**Ошибка 3:** Все цены = 0
- Проверьте, что вы авторизовались в Chrome (откройте WB вручную)
- Установите правильный адрес доставки на WB
- Перезапустите парсер

**Ошибка 4:** `ModuleNotFoundError: No module named '...'`
```powershell
# Решение: установите зависимости заново
pip install -r requirements.txt
```

---

### ШАГ 7: Проверить результаты

После завершения парсинга откройте файл с результатами:

```powershell
start data/prices_results.xlsx
```

**Что должно быть в файле:**
- ✅ Колонки: "ссылка на товар", "артикул", "цена"
- ✅ Данные по всем товарам
- ✅ Цены > 0 для доступных товаров
- ✅ Цены = 0 для недоступных товаров

**Если результаты некорректны:**
1. Проверьте авторизацию в Chrome (откройте WB вручную)
2. Проверьте адрес доставки на WB
3. Запустите парсер снова

---

## 🔄 Повторный запуск парсера

После первого запуска всё становится проще:

1. **Закройте Chrome:**
   ```powershell
   taskkill /F /IM chrome.exe
   ```

2. **Запустите парсер:**
   ```powershell
   python parsers/Parser_WB_Search.py
   ```

3. **Дождитесь завершения** (авторизация уже не нужна!)

4. **Проверьте результаты:**
   ```powershell
   start data/prices_results.xlsx
   ```

---

## ⚙️ Настройка режима работы

### Видимый режим (по умолчанию):
- Браузер открывается визуально
- Можно видеть процесс парсинга
- Удобно для первого запуска

### Фоновый режим (headless):
- Браузер работает в фоне (не виден)
- Не мешает работе за компьютером
- Быстрее работает

**Как включить фоновый режим:**

1. Откройте файл `parsers/Parser_WB_Search.py` в блокноте или Notepad++
2. Найдите строку:
   ```python
   HEADLESS_MODE = False
   ```
3. Измените на:
   ```python
   HEADLESS_MODE = True
   ```
4. Сохраните файл

⚠️ **Важно:** В фоновом режиме нельзя авторизоваться вручную. Используйте его только после первого успешного запуска в видимом режиме!

---

## 📊 Быстрая справка по командам

Сохраните эти команды - они понадобятся каждый раз:

```powershell
# 1. Перейти в папку проекта
cd D:\Projects\parserWBdoposleSPP

# 2. Закрыть Chrome перед запуском
taskkill /F /IM chrome.exe

# 3. Создать файл со ссылками (если изменили артикулы)
python parsers/Create_Links_Excel.py

# 4. Запустить парсер
python parsers/Parser_WB_Search.py

# 5. Открыть результаты
start data/prices_results.xlsx
```

---

## 🆘 Если ничего не помогает

### Вариант 1: Переустановка зависимостей

```powershell
# Удалить старые библиотеки
pip uninstall selenium undetected-chromedriver openpyxl -y

# Установить заново
pip install -r requirements.txt
```

### Вариант 2: Начать заново

1. Удалите папку проекта полностью
2. Скачайте проект заново:
   ```powershell
   git clone https://github.com/EvgeniyRibakov/parserWBdoposleSPP.git
   ```
3. Повторите все шаги с начала

### Вариант 3: Проверка версий

```powershell
# Проверьте версию Python (должна быть 3.8 или выше)
python --version

# Проверьте версию pip
pip --version
```

Если версия Python ниже 3.8, установите новую версию с [python.org](https://www.python.org/downloads/)

---

## 📞 Контакты для помощи

Если у вас возникли проблемы, которые не описаны в этом гайде:

1. Проверьте раздел "Решение проблем" в `README.md`
2. Проверьте документацию в папке `docs/`
3. Создайте Issue в репозитории GitHub

---

## ✅ Чек-лист установки

Отметьте выполненные шаги:

### Подготовка:
- [ ] Установлен Google Chrome
- [ ] Установлен Python 3.8+ (с галочкой "Add Python to PATH")
- [ ] Установлен Git

### Установка проекта:
- [ ] Проект скачан с GitHub (`git clone`)
- [ ] Файл `Парсер цен.xlsx` скопирован в папку `data/`
- [ ] Установлены зависимости (`pip install -r requirements.txt`)

### Первый запуск:
- [ ] Создан файл со ссылками (`python parsers/Create_Links_Excel.py`)
- [ ] Chrome закрыт перед запуском
- [ ] Парсер запущен (`python parsers/Parser_WB_Search.py`)
- [ ] Авторизован в Chrome (если нужно)
- [ ] Установлен адрес доставки на WB
- [ ] Парсинг завершён успешно
- [ ] Результаты проверены в `data/prices_results.xlsx`

---

## 💡 Полезные советы

1. **Сохраните команды:** Создайте текстовый файл с командами для быстрого доступа

2. **Проверяйте файлы перед запуском:** Убедитесь, что файл `Парсер цен.xlsx` содержит актуальные артикулы

3. **Делайте резервные копии:** Периодически копируйте папку `chrome_parser_profile/` - она содержит вашу авторизацию

4. **Не закрывайте PowerShell:** Во время работы парсера не закрывайте окно PowerShell

5. **Проверяйте результаты:** После каждого запуска проверяйте файл с результатами на наличие ошибок

---

**Удачи в работе! 🚀**

