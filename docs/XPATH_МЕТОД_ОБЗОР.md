# XPATH МЕТОД - КРАТКИЙ ОБЗОР

## ✅ ЧТО СДЕЛАНО

### 1. Новый парсер
**Файл:** `parsers/Parser_WB_XPATH.py`

**Принцип:** Парсит страницу продавца/бренда (100 товаров за раз)

### 2. Документация
**Файл:** `docs/ИНСТРУКЦИЯ_XPATH_ПАРСЕР.md`

### 3. Зависимости
Добавлена библиотека `lxml` в `requirements.txt`

---

## 🎯 АЛГОРИТМ РАБОТЫ

```
1. Открыть страницу продавца
   └─> https://www.wildberries.ru/seller/224650

2. Скроллить вниз до конца
   └─> Загружаются ВСЕ товары (lazy loading)

3. XPath запрос ко всем карточкам
   └─> Извлечь: артикул, 3 типа цен

4. Следующая страница (2, 3, 4...)
   └─> Повторить пункты 2-3

5. Готово! (450 товаров за ~1 минуту)
```

---

## 📊 КАКИЕ ДАННЫЕ СОБИРАЕТ

Для каждого товара:

| Поле | Описание | Пример |
|------|----------|--------|
| `Артикул` | ID товара на WB | 123456789 |
| `Цена ДО СПП` | Старая цена (зачёркнутая) | 2000 ₽ |
| `Цена ПОСЛЕ СПП` | Текущая цена со скидкой | 1500 ₽ |
| `Цена С КАРТОЙ` | Цена с картой WB | 1350 ₽ |

---

## 🚀 КАК ТЕСТИРОВАТЬ

### Вариант 1: Тестовый режим (РЕКОМЕНДУЕТСЯ)

```python
# В Parser_WB_XPATH.py установи:
TEST_MODE = True  # Только 1 страница
HEADLESS_MODE = False  # Видимый браузер

# Запусти:
python parsers/Parser_WB_XPATH.py
```

**Ожидаемый результат:**
- Откроется Chrome с твоим профилем
- Загрузится страница продавца
- Скроллинг до конца
- Парсинг первой страницы (~100 товаров)
- Сохранение в `data/prices_xpath_results.xlsx`

### Вариант 2: Полный парсинг

```python
TEST_MODE = False  # Все страницы
```

---

## 🔧 ЧТО НУЖНО НАСТРОИТЬ

### Обязательно:

1. **Ссылки на продавцов**
```python
SELLER_URLS = [
    "https://www.wildberries.ru/seller/224650",  # Твой кабинет 1
    # Добавь остальные 5 кабинетов
]
```

### Опционально:

2. **Профиль Chrome** (если не Profile 4)
```python
SOURCE_PROFILE_FOR_COPY = "Profile 4"  # Или "Default", "Profile 1"...
```

---

## ⚡ ПРЕИМУЩЕСТВА НОВОГО МЕТОДА

| Старый метод | XPATH метод |
|--------------|-------------|
| Открывает каждый товар отдельно | Парсит всю страницу сразу |
| 450 товаров = 450 запросов | 450 товаров = 5 запросов |
| ~10 минут | ~1 минута |
| 1 цена | 3 цены |
| Высокий риск блокировки | Низкий риск |

---

## 🐛 ПЕРВЫЙ ТЕСТ - ЧТО ПРОВЕРИТЬ

После первого запуска проверь:

### ✅ Парсер работает если:
- [x] Chrome открылся с твоим профилем
- [x] Страница продавца загрузилась
- [x] Скроллинг прошел успешно
- [x] Найдены карточки товаров
- [x] Извлечены артикулы и цены
- [x] Создан файл `prices_xpath_results.xlsx`

### ❌ Проблемы:

**"Карточки не найдены"**
→ WB изменил HTML структуру
→ Нужно обновить XPath селекторы

**"Captcha"**
→ Реши вручную (парсер подождёт 30 сек)

**"Цены = 0"**
→ Селекторы цен устарели
→ Нужно обновить через DevTools

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. **Первый тест**
   ```bash
   pip install lxml
   python parsers/Parser_WB_XPATH.py
   ```

2. **Проверить результат**
   - Открой `data/prices_xpath_results.xlsx`
   - Убедись что артикулы и цены корректные

3. **Сравнить со старым методом**
   - Запусти старый парсер на тех же товарах
   - Сравни цены (должны совпадать)

4. **Полный запуск**
   - Отключи `TEST_MODE`
   - Добавь все 6 кабинетов
   - Запусти парсинг

---

## 🎯 ВАЖНЫЕ МОМЕНТЫ

### Зачем скроллить вниз?

WB использует **Lazy Loading** (ленивую загрузку):
- Изначально в DOM только 20-30 товаров
- При скролле подгружаются еще 20-30
- **Без скролла** XPath найдет только первые 20!

### Почему страница продавца?

На странице продавца:
- ✅ Все товары одного кабинета
- ✅ Видны все цены сразу
- ✅ Естественное поведение пользователя
- ✅ Меньше риск блокировки

На странице товара:
- ❌ Нужно открывать каждый отдельно
- ❌ Много запросов
- ❌ Медленно

---

## 💡 СОВЕТЫ ПО ОПТИМИЗАЦИИ

### Параллельный парсинг 6 кабинетов

**Способ 1: Последовательно**
```python
SELLER_URLS = [url1, url2, url3, url4, url5, url6]
# Время: ~6 минут
```

**Способ 2: Параллельно** (БЫСТРЕЕ!)
- Запусти 6 копий парсера
- В каждом укажи свой URL
- Время: ~1 минута! 🔥

### Headless режим

После первой авторизации можешь использовать:
```python
HEADLESS_MODE = True  # Браузер не виден, работает быстрее
```

---

## 📞 НУЖНА ПОМОЩЬ?

Если что-то не работает:

1. Проверь логи в консоли
2. Сделай скриншот ошибки
3. Открой страницу продавца в Chrome вручную
4. Проверь структуру HTML (F12 → Elements)
5. Обнови XPath селекторы если нужно

---

**Готов к тестированию! 🚀**

Установи зависимости и запусти:
```bash
pip install lxml
python parsers/Parser_WB_XPATH.py
```




