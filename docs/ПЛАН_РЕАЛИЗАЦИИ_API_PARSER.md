# ПЛАН РЕАЛИЗАЦИИ: Высокоскоростной API-парсер цен

## 📋 УТОЧНЕННЫЕ ТРЕБОВАНИЯ

### Контекст
- **Приоритет:** ЛК покупателя ВБ (личный кабинет), а не seller-кабинет
- **WB:** 6 кабинетов с API-ключами (COSMO, MMA, MAB, MAU, DREAMLAB, BEAUTYLAB)
- **Ozon:** 3 кабинета с API-ключами и ID
- **Структура:** Один артикул = один товар
- **Требования:** Скорость И точность, нельзя пропускать товары

### Типы цен
- **WB:** Цена после СПП (без карты лояльности) - только число, без процента
- **Ozon:** Цена с картой лояльности (озон карта, зеленая цена)

### Обработка ошибок
- Недоступные товары → помечать "Недоступный товар"
- Критичная ошибка API → логи и остановка работы

---

## 🔧 ЧТО ТАКОЕ MITMPROXY?

**mitmproxy** — инструмент для перехвата и анализа HTTP/HTTPS трафика.

### Зачем нужен?
- Перехватывает все запросы браузера к серверам
- Позволяет видеть скрытые API-запросы
- Можно модифицировать запросы и ответы

### Альтернатива (проще)
**DevTools Network tab** — встроен в браузер, не требует установки:
1. Открыть браузер (Chrome/Firefox)
2. Нажать F12 → вкладка "Network"
3. Открыть карточку товара
4. Видеть все запросы в реальном времени

**Рекомендация:** Начать с DevTools, mitmproxy использовать только если нужно.

---

## 🚀 ПЛАН РЕАЛИЗАЦИИ

### ФАЗА 1: Discovery внутренних API (1-2 недели)

#### Этап 1.1: Анализ текущих решений
- [x] Изучить Parser_WB_API_FAST.py
- [x] Понять, какие данные уже получаются через официальный API
- [x] Определить пробелы: цена после СПП (без карты) для WB

**Выводы:**
- Официальный API WB дает `clubDiscountedPrice` (цена ПОСЛЕ СПП + скидки кошелька)
- Нужна цена ПОСЛЕ СПП БЕЗ карты (промежуточное значение)
- Возможно, нужно перехватить запрос из ЛК покупателя

#### Этап 1.2: Discovery WB через DevTools
**Действия:**
1. Открыть Chrome → F12 → Network
2. Авторизоваться в ЛК покупателя WB
3. Открыть карточку товара с СПП
4. Найти запросы, которые возвращают цены
5. Особое внимание на:
   - Запросы к `*.wildberries.ru`
   - GraphQL запросы
   - Запросы с параметрами товара (nmID/артикул)

**Ожидаемый результат:**
- Эндпоинт для получения цены после СПП (без карты)
- Структура запроса (headers, cookies, body)
- Формат ответа

#### Этап 1.3: Discovery Ozon
**Аналогично WB:**
1. DevTools → Network
2. Открыть карточку товара с картой лояльности
3. Найти запрос, который возвращает "зеленую цену"
4. Документировать эндпоинт

#### Этап 1.4: Документирование
Создать `docs/API_ENDPOINTS.md`:
- Эндпоинты WB (цена после СПП без карты)
- Эндпоинты Ozon (цена с картой)
- Примеры запросов и ответов
- Требования к аутентификации

---

### ФАЗА 2: Прототипирование (1 неделя)

#### Этап 2.1: Модуль для WB внутренних API
**Структура:**
```
parsers/api/
├── __init__.py
├── wb/
│   ├── __init__.py
│   ├── official_api.py    # Официальный API (уже есть)
│   ├── internal_api.py    # Внутренние API из ЛК покупателя
│   └── session_manager.py # Управление сессиями
```

**Функциональность:**
- Получение цены после СПП (без карты) через внутренний API
- Использование cookies из браузера или создание сессии
- Батчинг запросов (50-100 SKU за раз)

#### Этап 2.2: Модуль для Ozon внутренних API
**Аналогичная структура:**
```
parsers/api/
└── ozon/
    ├── __init__.py
    ├── official_api.py
    ├── internal_api.py    # Внутренние API для цены с картой
    └── session_manager.py
```

#### Этап 2.3: Тестирование
- Тест на 10-20 SKU для каждого маркетплейса
- Проверка точности (сравнение с ручным парсингом)
- Проверка обработки недоступных товаров

---

### ФАЗА 3: Оптимизация и унификация (1 неделя)

#### Этап 3.1: Батчинг и параллелизация
**Стратегия:**
- Группировка SKU по 50-100 штук
- Параллельные запросы через asyncio/aiohttp
- Ограничение concurrent запросов (10-20 одновременно)

**Реализация:**
```python
async def batch_get_prices(sku_list, batch_size=50):
    batches = [sku_list[i:i+batch_size] 
               for i in range(0, len(sku_list), batch_size)]
    
    async with aiohttp.ClientSession() as session:
        tasks = [get_prices_batch(session, batch) 
                 for batch in batches]
        results = await asyncio.gather(*tasks)
    return results
```

#### Этап 3.2: Унифицированный парсер
**Создать единый интерфейс:**
```python
class UnifiedPriceParser:
    def __init__(self):
        self.wb_parser = WBAPIParser()
        self.ozon_parser = OzonAPIParser()
    
    def parse_all(self, wb_skus, ozon_skus):
        # Параллельная обработка WB и Ozon
        wb_results = await self.wb_parser.parse_batch(wb_skus)
        ozon_results = await self.ozon_parser.parse_batch(ozon_skus)
        return self.merge_results(wb_results, ozon_results)
```

#### Этап 3.3: Обработка ошибок
- Недоступные товары → помечать "Недоступный товар"
- Критичная ошибка API → логировать и остановить работу
- Retry для временных ошибок (сетевые проблемы)

---

### ФАЗА 4: Интеграция и тестирование (1 неделя)

#### Этап 4.1: Форматирование вывода
- Единый формат данных для обоих маркетплейсов
- Сохранение в Excel (как сейчас)
- Колонки:
  - Артикул
  - Название товара
  - Цена со скидкой
  - Цена после СПП (WB) / Цена с картой (Ozon)
  - Статус (доступен/недоступен)

#### Этап 4.2: Полное тестирование
- Тест на всех 1600 SKU
- Проверка скорости (цель: 5-10 минут)
- Проверка точности (отклонение не более 1-2%)
- Проверка обработки всех товаров (не пропускать)

#### Этап 4.3: Логирование
- Детальные логи всех запросов
- Метрики производительности
- Логи ошибок с полным контекстом

---

## 📊 ТЕХНИЧЕСКИЙ СТЕК

### Библиотеки
- `aiohttp` — асинхронные HTTP запросы
- `requests` — синхронные запросы (fallback)
- `openpyxl` — работа с Excel
- `asyncio` — асинхронность
- `loguru` — улучшенное логирование

### Структура проекта
```
parsers/
├── api/
│   ├── __init__.py
│   ├── base.py              # Базовый класс
│   ├── wb/
│   │   ├── __init__.py
│   │   ├── official_api.py  # Официальный API
│   │   ├── internal_api.py  # Внутренние API из ЛК
│   │   └── session_manager.py
│   └── ozon/
│       ├── __init__.py
│       ├── official_api.py
│       ├── internal_api.py
│       └── session_manager.py
└── unified/
    ├── __init__.py
    └── parser.py            # Унифицированный парсер
```

---

## ⏱️ ОЦЕНКА ВРЕМЕНИ

| Фаза | Время | Статус |
|------|-------|--------|
| Фаза 1: Discovery | 1-2 недели | Начать немедленно |
| Фаза 2: Прототипирование | 1 неделя | После Фазы 1 |
| Фаза 3: Оптимизация | 1 неделя | После Фазы 2 |
| Фаза 4: Тестирование | 1 неделя | После Фазы 3 |
| **ИТОГО** | **4-5 недель** | |

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Немедленно:**
   - Начать Фазу 1: Discovery через DevTools
   - Открыть ЛК покупателя WB → F12 → Network
   - Открыть карточку товара с СПП
   - Найти запросы с ценами

2. **В течение недели:**
   - Завершить discovery для WB
   - Начать discovery для Ozon
   - Документировать найденные эндпоинты

3. **В течение месяца:**
   - Рабочий прототип для обоих маркетплейсов
   - Тестирование на реальных данных
   - Оптимизация скорости

---

**Дата обновления:** 2025-01-27  
**Ветка:** `apisparse`  
**Статус:** Готов к началу реализации

