# ПАРСЕР ЦЕН WILDBERRIES - ДОПАРСИНГ НЕДОСТАЮЩИХ АРТИКУЛОВ

Автономная версия парсера для обработки конкретного списка артикулов.

## 📦 СОДЕРЖИМОЕ ПАПКИ

```
parser_missing_standalone/
├── Parser_WB_Missing.py         ← Основной скрипт парсера
├── requirements.txt              ← Список зависимостей Python
├── .env_sample                   ← Пример файла настроек
├── README.md                     ← Подробная инструкция (этот файл)
├── QUICK_START.txt               ← Быстрый старт (краткая инструкция)
├── ПЕРЕДАЧА_НА_НОВЫЙ_ПК.txt     ← Инструкция по передаче на новый ПК
├── INSTALL.bat                   ← Скрипт установки зависимостей (Windows)
├── RUN.bat                       ← Скрипт запуска парсера (Windows)
└── data/                         ← Папка для результатов (создается автоматически)
    └── missing_articles_results.xlsx  ← Результаты парсинга
```

## 🚀 БЫСТРЫЙ СТАРТ

### Шаг 1: Установка Python

1. **Скачайте Python 3.10 или новее:**
   - Перейдите на https://www.python.org/downloads/
   - Скачайте последнюю версию Python 3.x для Windows
   - **ВАЖНО:** При установке отметьте галочку "Add Python to PATH"

2. **Проверьте установку:**
   ```powershell
   python --version
   ```
   Должно показать версию Python (например, `Python 3.12.0`)

### Шаг 2: Установка Google Chrome

1. **Установите Google Chrome** (если еще не установлен):
   - Скачайте с https://www.google.com/chrome/
   - Установите стандартным способом

2. **Проверьте установку:**
   - Chrome должен открываться и работать нормально

### Шаг 3: Установка зависимостей Python

**Вариант А (простой):**
1. Дважды кликните на файл `INSTALL.bat`
2. Дождитесь завершения установки

**Вариант Б (вручную):**
1. Откройте PowerShell или командную строку в папке `parser_missing_standalone`
2. Установите зависимости:
   ```powershell
   pip install -r requirements.txt
   ```
   
   Если команда `pip` не найдена, попробуйте:
   ```powershell
   python -m pip install -r requirements.txt
   ```

3. Дождитесь завершения установки (может занять несколько минут)

### Шаг 4: Настройка Google Таблиц (рекомендуется)

1. **Создайте Service Account в Google Cloud Console:**
   - Перейдите на https://console.cloud.google.com/
   - Создайте проект (или выберите существующий)
   - Включите Google Sheets API
   - Создайте Service Account и скачайте JSON файл
   - Сохраните файл как `google-credentials.json` в папке `parser_missing_standalone`

2. **Поделитесь Google Таблицей с email из Service Account:**
   - Откройте вашу Google Таблицу
   - Нажмите "Настройки доступа" → "Добавить пользователя"
   - Вставьте email из `google-credentials.json` (поле `client_email`)
   - Дайте права "Редактор"

### Шаг 5: Настройка файла .env (опционально)

1. **Скопируйте файл `.env_sample` в `.env`:**
   ```powershell
   copy .env_sample .env
   ```

2. **Откройте файл `.env` в блокноте** и при необходимости измените настройки:
   - `HEADLESS_MODE=True` - фоновый режим (браузер не виден)
   - `HEADLESS_MODE=False` - видимый режим (браузер виден, можно авторизоваться)
   - `PARALLEL_TABS=20` - количество вкладок одновременно (рекомендуется 20)
   - `SOURCE_PROFILE_FOR_COPY=Profile 4` - имя профиля Chrome для копирования данных
   - `GOOGLE_SHEETS_ENABLED=True` - включить сохранение в Google Таблицы
   - `GOOGLE_SHEET_URL=...` - ссылка на вашу Google Таблицу
   - `GOOGLE_SHEET_NAME=Лист1` - имя листа в таблице

   **Примечание:** Если файл `.env` не создан, скрипт будет использовать настройки по умолчанию.

### Шаг 6: Запуск парсера

**Вариант А (простой):**
1. Убедитесь, что Chrome закрыт
2. Дважды кликните на файл `RUN.bat`
3. Следуйте инструкциям на экране

**Вариант Б (вручную):**
1. Убедитесь, что Chrome закрыт (или используйте remote режим)
2. Запустите скрипт:
   ```powershell
   python Parser_WB_Missing.py
   ```

3. **Следуйте инструкциям на экране:**
   - Скрипт откроет браузер
   - Авторизуйтесь в Wildberries
   - Установите адрес доставки
   - Нажмите ENTER в консоли
   - Разрешите открытие вкладок в браузере (когда появится запрос)
   - Нажмите ENTER в консоли еще раз
   - Парсер начнет работу

4. **Результаты сохраняются:**
   - В Google Таблицы (если настроено) - **после каждых 20 товаров**
   - В Excel файл `data/missing_articles_results.xlsx` - в конце работы

## 📋 ПОДРОБНАЯ ИНСТРУКЦИЯ

### Что делает скрипт?

1. Открывает Chrome браузер
2. Запрашивает авторизацию в Wildberries (если не авторизован)
3. Открывает тестовую вкладку для подтверждения разрешения на открытие вкладок
4. Открывает все товары параллельно (по 20 вкладок одновременно)
5. Парсит цены с каждой страницы товара
6. **Сохраняет результаты в Google Таблицы после каждых 20 товаров** (если настроено)
7. Сохраняет результаты в Excel файл в конце работы

### Структура результатов

Результаты сохраняются в Excel файл `data/missing_articles_results.xlsx` с 4 столбцами:

| Столбец | Описание |
|---------|----------|
| ссылка на товар | Полная ссылка на товар на Wildberries |
| артикул | Артикул товара |
| цена | Обычная цена товара |
| цена с картой | Цена товара с картой WB |

### Настройки в .env файле

#### Настройки браузера:

- `BROWSER_TYPE=chrome` - тип браузера (chrome или edge)
- `HEADLESS_MODE=True` - фоновый режим (True = не виден, False = виден)
- `USE_REMOTE_CHROME=False` - использовать remote Chrome (False = обычный запуск)
- `USE_TEMP_PROFILE=True` - использовать временный профиль
- `COPY_PROFILE_DATA=True` - копировать данные из профиля Chrome
- `SOURCE_PROFILE_FOR_COPY=Profile 4` - имя профиля для копирования
- `WAIT_FOR_MANUAL_LOGIN=True` - ждать ручной авторизации
- `MANUAL_LOGIN_TIMEOUT=120` - таймаут авторизации в секундах

#### Настройки парсинга:

- `PARALLEL_TABS=20` - количество вкладок одновременно (рекомендуется 20)

#### Настройки Google Таблиц:

- `GOOGLE_SHEETS_ENABLED=True` - включить сохранение в Google Таблицы
- `GOOGLE_SHEET_URL=...` - ссылка на Google Таблицу
- `GOOGLE_SHEET_NAME=Лист1` - имя листа в таблице
- `GOOGLE_SERVICE_ACCOUNT_FILE=google-credentials.json` - путь к файлу Service Account

### Режимы работы

#### Видимый режим (HEADLESS_MODE=False):
- ✅ Браузер открывается визуально
- ✅ Можно видеть процесс парсинга
- ✅ Можно авторизоваться вручную при первом запуске
- ✅ Удобно для отладки

#### Фоновый режим (HEADLESS_MODE=True):
- ✅ Браузер работает в фоне (не виден)
- ✅ Не мешает работе за компьютером
- ✅ Быстрее работает
- ⚠️ Требует готовый авторизованный профиль

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "python не является внутренней или внешней командой"

**Решение:**
1. Переустановите Python с галочкой "Add Python to PATH"
2. Или используйте полный путь: `C:\Users\ВашеИмя\AppData\Local\Programs\Python\Python312\python.exe`

### Проблема: "pip не является внутренней или внешней командой"

**Решение:**
```powershell
python -m pip install -r requirements.txt
```

### Проблема: "Chrome не запускается"

**Решение:**
1. Убедитесь что Chrome установлен
2. Закройте все окна Chrome перед запуском скрипта
3. Попробуйте запустить Chrome вручную и закрыть его

### Проблема: "Вкладки не открываются"

**Решение:**
1. Убедитесь что вы разрешили открытие вкладок в браузере (когда появился запрос)
2. Попробуйте запустить в видимом режиме (`HEADLESS_MODE=False`)
3. Проверьте настройки безопасности браузера

### Проблема: "Ошибка при сохранении Excel"

**Решение:**
1. Убедитесь что файл `data/missing_articles_results.xlsx` не открыт в Excel
2. Закройте Excel перед запуском скрипта
3. Проверьте права доступа к папке `data/`

## 📤 ПЕРЕДАЧА НА НОВЫЙ ПК

### Что нужно передать:

1. **Всю папку `parser_missing_standalone/`** со всеми файлами:
   - `Parser_WB_Missing.py`
   - `requirements.txt`
   - `.env_sample`
   - `README.md`

2. **Опционально:** Профиль Chrome `chrome_parser_profile/` (если используете авторизованный профиль)

### Как передать:

1. **Через USB флешку:**
   - Скопируйте папку `parser_missing_standalone/` на флешку
   - На новом ПК скопируйте папку в удобное место (например, `C:\Parser\`)

2. **Через облако (Google Drive, Яндекс.Диск и т.д.):**
   - Заархивируйте папку `parser_missing_standalone/` в ZIP
   - Загрузите в облако
   - На новом ПК скачайте и распакуйте

3. **Через сеть:**
   - Скопируйте папку по сети на новый ПК

### Что НЕ нужно передавать:

- ❌ Папку `data/` с результатами (создастся автоматически)
- ❌ Папку `chrome_parser_profile/` (создастся автоматически при первом запуске)
- ❌ Файл `.env` (можно создать из `.env_sample`)

## 📝 ПРИМЕР ИСПОЛЬЗОВАНИЯ

### Первый запуск:

```powershell
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Создайте файл .env (опционально)
copy .env_sample .env

# 3. Запустите скрипт
python Parser_WB_Missing.py

# 4. Следуйте инструкциям на экране:
#    - Авторизуйтесь в Wildberries
#    - Разрешите открытие вкладок
#    - Дождитесь завершения парсинга
```

### Повторный запуск:

```powershell
# Просто запустите скрипт
python Parser_WB_Missing.py
```

## 📊 РЕЗУЛЬТАТЫ

После завершения парсинга результаты будут сохранены в файл:

```
data/missing_articles_results.xlsx
```

Файл можно открыть в Excel или Google Sheets для дальнейшей обработки.

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Зависимости:

- `selenium` - автоматизация браузера
- `undetected-chromedriver` - обход защиты от ботов
- `openpyxl` - работа с Excel файлами
- `python-dotenv` - загрузка настроек из .env
- `requests` - проверка доступности remote Chrome
- `gspread` - работа с Google Sheets API
- `google-auth` - аутентификация Google
- `google-auth-httplib2` - HTTP транспорт для Google Auth

### Системные требования:

- **ОС:** Windows 10/11
- **Python:** 3.10 или новее
- **Chrome:** последняя версия
- **RAM:** минимум 4 ГБ (рекомендуется 8 ГБ)
- **Интернет:** стабильное подключение

### Производительность:

- **Скорость:** ~20 товаров за раз (параллельно)
- **Время:** ~1-2 секунды на товар
- **Всего артикулов:** 59 (жестко задано в коде)
- **Автосохранение:** каждые 20 товаров в Google Таблицы

## 📞 ПОДДЕРЖКА

При возникновении проблем:

1. Проверьте что все зависимости установлены
2. Убедитесь что Chrome установлен и работает
3. Проверьте настройки в файле `.env`
4. Попробуйте запустить в видимом режиме для отладки

## 📄 ЛИЦЕНЗИЯ

Этот скрипт создан для внутреннего использования.

