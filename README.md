# Парсер цен Wildberries

Автоматизированный парсер цен товаров с Wildberries для анализа цен до и после СПП.

## 📖 О проекте

### Зачем нужен этот парсер?

Парсер автоматизирует сбор цен товаров с Wildberries для:
- 📊 Анализа цен до и после СПП (Специальной Программы Продавца)
- 📈 Отслеживания изменений цен конкурентов
- 💰 Мониторинга цен на больших объемах товаров (900+ артикулов)
- ⏱️ Экономии времени (автоматизация вместо ручного сбора)

### Что делает парсер?

1. **Читает артикулы** из Excel файла (`data/Парсер цен.xlsx`)
2. **Открывает карточки товаров** на Wildberries через браузер
3. **Извлекает финальные цены** (с учетом всех скидок и СПП)
4. **Сохраняет результаты** в Excel файл (`data/prices_results.xlsx`)

### Как работает?

**Основной парсер (`Parser_WB_Search.py`)**:
- Использует **Selenium** + **undetected-chromedriver** для имитации реального браузера
- Обходит защиту от ботов Wildberries
- Работает через Chrome с вашим профилем (авторизация, адрес доставки)
- Открывает товары в новых вкладках одного окна
- Кликает на кнопку "Цена с WB Кошельком" для получения финальной цены
- Пропускает недоступные товары автоматически
- **Поддерживает headless режим** (фоновый, без визуального окна браузера)

**Альтернативные парсеры**:
- `Parser_WB_API_FAST.py` - быстрый парсер через API (требует токены)
- `Parser_WB_Card_API.py` - парсер через Card API
- Другие варианты для разных сценариев

### 🔇 Headless режим (фоновый, без визуального браузера)

Парсер поддерживает **два режима работы**:

#### Видимый режим (по умолчанию):
- Браузер открывается визуально
- Можно видеть процесс парсинга
- Можно авторизоваться вручную при первом запуске
- Удобно для отладки и первого запуска

#### Headless режим (фоновый):
- Браузер работает в фоне (не виден)
- Не мешает работе за компьютером
- Быстрее работает (меньше нагрузка на графику)
- **Требует готовый авторизованный профиль** (нельзя авторизоваться вручную)

**Как включить headless режим:**

Открой `parsers/Parser_WB_Search.py` и измени:
```python
HEADLESS_MODE = True  # True = фоновый, False = видимый
```

**Важно:**
- ⚠️ В headless режиме **нельзя авторизоваться вручную**
- ✅ Используй готовый профиль `chrome_parser_profile/` (скопированный с рабочего ПК)
- ✅ Или сначала авторизуйся в видимом режиме, затем переключись на headless

---

## 🚀 Развертывание на новом ПК

### Шаг 1: Что уже есть в GitHub (клонируем репозиторий)

```bash
git clone https://github.com/EvgeniyRibakov/parserWBdoposleSPP.git
cd parserWBdoposleSPP
```

**Что уже в репозитории:**
- ✅ `parsers/` - весь код парсеров
- ✅ `docs/` - вся документация
- ✅ `code_pages/` - примеры HTML для разработки
- ✅ `requirements.txt` - список зависимостей Python
- ✅ `.gitignore` - настройки игнорирования файлов
- ✅ `README.md` - этот файл

### Шаг 2: Что нужно передать локально (вручную)

Эти файлы **НЕ** в GitHub (личные данные, результаты):

#### 📁 Папка `data/`:
```
data/
├── Парсер цен.xlsx          ← ⚠️ НУЖНО ПЕРЕДАТЬ ВРУЧНУЮ
│   └── Лист "Данные для парсера ВБ" с артикулами
│
├── links_to_products.xlsx   ← Генерируется автоматически (можно не передавать)
└── prices_results.xlsx      ← Результаты (можно не передавать)
```

**Как передать:**
- Скопируй файл `data/Парсер цен.xlsx` с рабочего ПК на новый
- Убедись, что в файле есть лист "Данные для парсера ВБ" с колонкой артикулов

#### 🔐 Файл `.env` (если используешь API парсеры):

```
.env                          ← ⚠️ НУЖНО ПЕРЕДАТЬ ВРУЧНУЮ (если используешь API парсеры)
```

**Содержимое `.env`** (пример):
```env
WB_API_KEY_COSMO=твой_ключ_1
WB_API_KEY_MMA=твой_ключ_2
WB_API_KEY_MAB=твой_ключ_3
WB_API_KEY_MAU=твой_ключ_4
WB_API_KEY_DREAMLAB=твой_ключ_5
WB_API_KEY_BEAUTYLAB=твой_ключ_6
```

**Важно**: 
- Файл `.env` находится в **корне проекта** (рядом с `README.md`)
- Нужен только если используешь `Parser_WB_API_FAST.py` или другие API парсеры
- Для основного парсера `Parser_WB_Search.py` НЕ нужен

#### 📁 Папка `chrome_parser_profile/` (опционально):

```
chrome_parser_profile/        ← ⚠️ ОПЦИОНАЛЬНО (можно не передавать)
├── Network/Cookies          ← Авторизация на WB
├── Preferences              ← Адрес доставки
└── ...
```

**Варианты:**
1. **Не передавать** - парсер создаст новый профиль автоматически, нужно будет авторизоваться вручную (1 раз)
2. **Передать готовый** - скопировать всю папку `chrome_parser_profile/` с рабочего ПК (экономит время на авторизацию)

**Как передать:**
- Скопируй папку `chrome_parser_profile/` целиком с рабочего ПК на новый
- Убедись, что Chrome закрыт на обоих ПК при копировании

### Шаг 3: Установка на новом ПК

#### 3.1. Установка Python

**Требования**: Python 3.8 или выше

**Проверка версии:**
```bash
python --version
# или
python3 --version
```

**Если Python не установлен:**
- Скачай с [python.org](https://www.python.org/downloads/)
- При установке отметь "Add Python to PATH"
- Перезапусти терминал после установки

#### 3.2. Установка Google Chrome

**Требования**: Google Chrome браузер (любая актуальная версия)

**Проверка:**
- Открой Chrome → Настройки → О браузере
- Должна быть установлена актуальная версия

**Если Chrome не установлен:**
- Скачай с [google.com/chrome](https://www.google.com/chrome/)

**Важно**: 
- Chrome должен быть установлен в стандартную папку:
  - `C:\Program Files\Google\Chrome\Application\chrome.exe`
  - Или `C:\Program Files (x86)\Google\Chrome\Application\chrome.exe`

#### 3.3. Установка зависимостей Python

Открой терминал в папке проекта и выполни:

```bash
# Установка всех зависимостей из requirements.txt
pip install -r requirements.txt
```

**Что установится:**
- `selenium` - автоматизация браузера
- `undetected-chromedriver` - обход защиты от ботов
- `openpyxl` - работа с Excel файлами
- `requests` - HTTP запросы (для API парсеров)
- `python-dotenv` - загрузка .env файлов
- `webdriver-manager` - автоматическая установка драйверов

**Если возникают ошибки:**
```bash
# Попробуй с правами администратора
pip install --upgrade pip
pip install -r requirements.txt

# Или используй python -m pip
python -m pip install -r requirements.txt
```

#### 3.4. Проверка установки

```bash
# Проверь, что все установилось
python -c "import selenium; import undetected_chromedriver; import openpyxl; print('✅ Все зависимости установлены')"
```

### Шаг 4: Первый запуск

#### 4.1. Подготовка данных

```bash
# Создать файл со ссылками из артикулов
python parsers/Create_Links_Excel.py
```

**Что произойдет:**
- Парсер прочитает артикулы из `data/Парсер цен.xlsx`
- Создаст файл `data/links_to_products.xlsx` со ссылками на товары

#### 4.2. Настройка режима работы (опционально)

Открой `parsers/Parser_WB_Search.py` и настрой режим:

```python
# Режим работы браузера
HEADLESS_MODE = False  # True = фоновый (без окна), False = видимый
```

**Рекомендации:**
- **Первый запуск**: `HEADLESS_MODE = False` (видимый) - для авторизации
- **Последующие запуски**: `HEADLESS_MODE = True` (фоновый) - не мешает работе

#### 4.3. Запуск основного парсера

```bash
# Запуск основного парсера
python parsers/Parser_WB_Search.py
```

**Что произойдет при первом запуске:**

1. **Создание Chrome профиля** (если не передал готовый):
   - Парсер создаст папку `chrome_parser_profile/`
   - Скопирует данные из твоего Chrome Profile 4 (cookies, настройки)
   - Откроет Chrome с новым профилем

2. **Пауза для авторизации** (если профиль новый и `HEADLESS_MODE = False`):
   ```
   ⏸  ПАУЗА ДЛЯ АВТОРИЗАЦИИ
   
   📋 ИНСТРУКЦИЯ:
      1. В открывшемся Chrome зайдите на сайт WB
      2. Авторизуйтесь в своем аккаунте
      3. Установите правильный адрес доставки
      4. Нажмите ENTER когда будете готовы
   ```
   
   **В headless режиме** (`HEADLESS_MODE = True`):
   - Пауза для авторизации пропускается
   - Убедись, что профиль уже авторизован

3. **Парсинг товаров**:
   - Парсер откроет каждый товар в новой вкладке
   - Извлечет цену
   - Закроет вкладку
   - Сохранит результат

4. **Результаты**:
   - Сохраняются в `data/prices_results.xlsx`
   - Формат: артикул, ссылка, цена

### Шаг 5: Проверка работы

После первого запуска проверь:

1. ✅ Файл `data/prices_results.xlsx` создан
2. ✅ В файле есть результаты парсинга
3. ✅ Цены извлечены корректно
4. ✅ Chrome профиль создан и авторизован

---

## 📋 Чек-лист развертывания

### На рабочем ПК (откуда копируем):

- [ ] Скопировать `data/Парсер цен.xlsx`
- [ ] Скопировать `.env` (если используешь API парсеры)
- [ ] Опционально: скопировать `chrome_parser_profile/` (экономит время)

### На новом ПК (куда устанавливаем):

- [ ] Клонировать репозиторий: `git clone ...`
- [ ] Установить Python 3.8+
- [ ] Установить Google Chrome
- [ ] Установить зависимости: `pip install -r requirements.txt`
- [ ] Скопировать `data/Парсер цен.xlsx`
- [ ] Скопировать `.env` (если нужен)
- [ ] Опционально: скопировать `chrome_parser_profile/`
- [ ] Запустить: `python parsers/Create_Links_Excel.py`
- [ ] Запустить: `python parsers/Parser_WB_Search.py`
- [ ] Авторизоваться в Chrome (если профиль новый)
- [ ] Проверить результаты в `data/prices_results.xlsx`

---

## 🔧 Структура проекта

```
wb-beforeafter-spp-prices/
├── parsers/                    # Все парсеры и скрипты
│   ├── Parser_WB_Search.py    # ⭐ Основной парсер (Selenium)
│   ├── Parser_WB_API_FAST.py  # Быстрый парсер через API
│   ├── Create_Links_Excel.py  # Создание файла со ссылками
│   └── ...
│
├── docs/                       # Вся документация
│   ├── ПРАВИЛА_ПРОЕКТА.md     # ⭐ Главные правила проекта
│   ├── ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md
│   └── ...
│
├── data/                       # Excel файлы с данными
│   ├── Парсер цен.xlsx         # ⚠️ Передать вручную
│   ├── links_to_products.xlsx  # Генерируется автоматически
│   └── prices_results.xlsx    # Результаты парсинга
│
├── code_pages/                 # Примеры HTML для парсинга
├── chrome_parser_profile/      # Рабочий профиль Chrome (автосоздается)
├── .env                        # ⚠️ Передать вручную (если нужен)
├── requirements.txt            # Зависимости Python
└── README.md                   # Этот файл
```

---

## 💡 Идеи для улучшения

### 🚀 Производительность

1. **Параллельный парсинг**
   - Запуск нескольких браузеров одновременно
   - Парсинг товаров параллельно (например, 3-5 вкладок одновременно)
   - Ускорение в 3-5 раз

2. **Кеширование результатов**
   - Сохранение уже спарсенных цен
   - Пропуск товаров, которые уже были обработаны
   - Полезно при повторных запусках

3. **Оптимизация браузера**
   - Отключение загрузки изображений
   - Блокировка рекламы и скриптов
   - Ускорение загрузки страниц

### 🎯 Функциональность

4. **Мониторинг изменений цен**
   - Сравнение цен с предыдущими запусками
   - Уведомления о значительных изменениях
   - Графики изменения цен

5. **Расширенная статистика**
   - Средняя цена по категориям
   - Минимальная/максимальная цена
   - Процент товаров со скидкой
   - Анализ динамики цен

6. **Поддержка нескольких магазинов**
   - Парсинг цен с разных аккаунтов WB
   - Сравнение цен между магазинами
   - Агрегация результатов

### 🛡️ Надежность

7. **Улучшенная обработка ошибок**
   - Автоматический retry при ошибках
   - Сохранение прогресса при сбоях
   - Возобновление с места остановки

8. **Защита от блокировок**
   - Ротация User-Agent
   - Случайные задержки между запросами
   - Использование прокси (опционально)

9. **Логирование**
   - Подробные логи всех операций
   - Сохранение ошибок в файл
   - Статистика работы парсера

### 📊 Удобство использования

10. **Веб-интерфейс**
    - Простой веб-интерфейс для запуска парсера
    - Просмотр результатов в браузере
    - Настройка параметров через UI

11. **Конфигурационный файл**
    - Все настройки в одном файле (config.yaml)
    - Не нужно редактировать код
    - Легкая настройка под разные задачи

12. **Уведомления**
    - Email уведомления о завершении парсинга
    - Telegram бот для мониторинга
    - Push-уведомления

### 🔍 Аналитика

13. **Экспорт в разные форматы**
    - CSV для Excel
    - JSON для API
    - PDF отчеты

14. **Интеграция с базами данных**
    - Сохранение в PostgreSQL/MySQL
    - История всех парсингов
    - Быстрый поиск и фильтрация

15. **API для интеграции**
    - REST API для запуска парсера
    - Получение результатов через API
    - Интеграция с другими системами

### 🎨 UX/UI

16. **Прогресс-бар**
    - Визуальный прогресс парсинга
    - Оценка времени до завершения
    - Статистика в реальном времени

17. **Интерактивные отчеты**
    - Интерактивные графики цен
    - Фильтры и сортировка результатов
    - Экспорт выбранных данных

---

## 📚 Дополнительная документация

- **[ПРАВИЛА_ПРОЕКТА.md](docs/ПРАВИЛА_ПРОЕКТА.md)** - Главные правила и решения проекта
- **[ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md](docs/ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md)** - Подробная инструкция по использованию
- **[ПРАВИЛА_ПАРСИНГА_WB.md](docs/ПРАВИЛА_ПАРСИНГА_WB.md)** - Детали парсинга цен
- **[СТРУКТУРА_ПРОЕКТА.md](docs/СТРУКТУРА_ПРОЕКТА.md)** - Описание структуры проекта

---

## 🐛 Решение проблем

См. [ПРАВИЛА_ПРОЕКТА.md](docs/ПРАВИЛА_ПРОЕКТА.md) раздел "Решение частых проблем"

---

## 📄 Лицензия

Проект для внутреннего использования.

---

**Вопросы?** См. документацию в папке `docs/` или создай Issue в репозитории.
