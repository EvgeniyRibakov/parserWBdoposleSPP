# Парсер цен Wildberries

Автоматизированный парсер цен товаров с Wildberries для анализа цен до и после СПП.

## 📁 Структура проекта

```
wb-beforeafter-spp-prices/
├── parsers/                    # Все парсеры и скрипты
│   ├── Parser_WB_Search.py    # ⭐ Основной парсер (Selenium)
│   ├── Parser_WB_API_FAST.py  # Быстрый парсер через API
│   ├── Parser_WB_Card_API.py  # Парсер через Card API
│   ├── Parser_WB_ALL_PRODUCTS.py
│   ├── Parser_UNIFIED.py
│   ├── Create_Links_Excel.py  # Создание файла со ссылками
│   └── Step1_Load_All_IDs.py  # Загрузка всех артикулов
│
├── docs/                       # Вся документация
│   ├── ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md
│   ├── ИНСТРУКЦИЯ_БЫСТРЫЙ_ПАРСЕР.md
│   ├── ПРАВИЛА_ПРОЕКТА.md      # ⭐ Главные правила проекта
│   ├── ПРАВИЛА_ПАРСИНГА_WB.md
│   └── ПРОБЛЕМА_GIT_БОЛЬШИЕ_ФАЙЛЫ.md
│
├── data/                       # Excel файлы с данными
│   ├── Парсер цен.xlsx         # Входной файл с артикулами
│   ├── links_to_products.xlsx  # Файл со ссылками (генерируется)
│   └── prices_results.xlsx    # Результаты парсинга
│
├── code_pages/                 # Примеры HTML для парсинга
│   ├── elements/               # Элементы страниц
│   │   ├── tap_find_black_price.html
│   │   ├── copy_black_price.html
│   │   └── sold_out_product.html
│   └── pages/                  # Полные страницы
│       └── product_page_example.html
│
├── chrome_parser_profile/      # Рабочий профиль Chrome (автосоздается)
│   └── ...                     # Cookies, настройки, кеши
│
├── requirements.txt            # Зависимости Python
├── .gitignore                  # Игнорируемые файлы
└── README.md                   # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Подготовка данных
```bash
# Создать файл со ссылками из артикулов
python parsers/Create_Links_Excel.py
```

### 3. Запуск парсера
```bash
# Основной парсер (Selenium)
python parsers/Parser_WB_Search.py

# Или быстрый парсер через API
python parsers/Parser_WB_API_FAST.py
```

## 📚 Документация

- **[ПРАВИЛА_ПРОЕКТА.md](docs/ПРАВИЛА_ПРОЕКТА.md)** - Главные правила и решения проекта
- **[ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md](docs/ИНСТРУКЦИЯ_ВСЕ_ТОВАРЫ.md)** - Подробная инструкция по использованию
- **[ПРАВИЛА_ПАРСИНГА_WB.md](docs/ПРАВИЛА_ПАРСИНГА_WB.md)** - Детали парсинга цен

## 🔧 Основные парсеры

### `Parser_WB_Search.py` ⭐ (Рекомендуется)
- Использует Selenium + undetected-chromedriver
- Работает через браузер (как человек)
- Обходит защиту от ботов
- Поддерживает авторизацию через Chrome профиль

### `Parser_WB_API_FAST.py`
- Работает только через API (без браузера)
- Очень быстрый (секунды вместо минут)
- Требует токены авторизации

## 📝 Важные замечания

### ⚠️ Chrome профиль
- При первом запуске парсер создаст `chrome_parser_profile/`
- Нужно будет авторизоваться вручную (1 раз)
- Профиль НЕ коммитится в git (см. `.gitignore`)

### ⚠️ Excel файлы
- Все Excel файлы находятся в папке `data/`
- Парсеры автоматически находят их по относительным путям
- Результаты сохраняются в `data/prices_results.xlsx`

## 🐛 Решение проблем

См. [ПРАВИЛА_ПРОЕКТА.md](docs/ПРАВИЛА_ПРОЕКТА.md) раздел "Решение частых проблем"

## 📊 Результаты

Результаты парсинга сохраняются в:
- `data/prices_results.xlsx` - для основного парсера
- `data/Парсер цен.xlsx` (лист "Результаты") - для других парсеров

## 🔒 Безопасность

- Chrome профили и кеши **НЕ коммитятся** в git
- Excel файлы с результатами в `.gitignore`
- Личные данные (cookies, пароли) остаются локально

## 📄 Лицензия

Проект для внутреннего использования.

---

**Вопросы?** См. документацию в папке `docs/`

